import * as TYPES from "../actions/actiontypes";

//替换为当前reducer
export default function XXXReducer(
	state = {
		initData:"111"	//设置初始值
	},
	action
) {
	switch (action.type) {
        default:
            return state;
			break;
	}
}